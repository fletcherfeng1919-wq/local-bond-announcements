beautifulsoup4==4.15.0
chinese_calendar==1.11.0
cn2an==0.5.24
lxml==6.1.1
matplotlib==3.11.1
openpyxl==3.1.5
pandas==3.0.5
# playwright also needs its browser binary installed separately (pip install
# alone does not fetch it): `.venv/bin/playwright install chromium`. Used by
# src/provincial_crawl.py to render provincial finance-bureau listing pages
# that don't work via plain requests (JS-rendered or, in one case, a broken
# server route -- see that module's docstring).
pdfplumber==0.11.10
playwright==1.62.0
PyMuPDF==1.26.7
pytesseract==0.3.13
Pillow
python-dateutil==2.9.0.post0
python-docx==1.2.0
requests==2.34.2
soupsieve==2.9.1
